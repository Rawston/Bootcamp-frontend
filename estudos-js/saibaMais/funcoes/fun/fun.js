function sum(x, y) {
    return x + y;
}

function soma(x, y) {
    return x + y;
    console.log("Hello World"); //isso NUNCA vai rodar
}
/**
 * Complete a função soma de forma que ela retorne a soma de a e b.
 * @param {number} a
 * @param {number} b
 */
function soma(a, b) {
    return a + b;
}

// exemplo de uso - não modifique
console.log(sum(1, 3));
console.log(sum(2, 5));

/**
 * @param {number} a
 * @param {number} b
 */
function multiplique(a, b) {
    return a * b;
}

// exemplo de uso - não modifique
console.log(multiplique(2, 4));
console.log(multiplique(3, 2));